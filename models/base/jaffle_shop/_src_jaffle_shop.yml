sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    config:
      freshness:
        warn_after:
          count: 24
          period: hour
        error_after:
          count: 38
          period: hour
      loaded_at_field: _etl_loaded_at
    tables:
      - name: orders
      - name: customers